<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Compose. -->
    <!-- Since we're using Compose, this theme is minimal and mainly for system UI. -->
    <style name="Theme.NFCTagWriter" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.NFCTagWriter.NoActionBar" parent="Theme.NFCTagWriter">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.NFCTagWriter.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.NFCTagWriter.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>